#include <stdio.h>
main()
{
    const float pi=3.14;
    float r,a,c;
    printf("enter radius=");
    scanf("%f",&r);
    a=pi*r*r;
    c=2*pi*r;
    printf("area of circle is %0.3f and circumference of circle is %0.3f",a,c);
    
}
