#include <stdio.h>
int main()
{
    int a[5]={723,35,413,98,186};
    int i,large=a[0];
    for(i=0;i<5;i++)
    {
        if(a[i]>large)
        {
            large=a[i];
        }
    }
    printf("largest no. is %d",large);
}
